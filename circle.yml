machine:
  ruby:
    version: ruby-2.1.2
test:
  override:
    - bundle exec rspec -c -fd
deployment:
  production:
    branch: master
    commands:
      - git push git@heroku.com:cooper-rails.git $CIRCLE_SHA1:master
      - heroku run rake db:migrate --app cooper-rails
